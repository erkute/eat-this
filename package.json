{
  "name": "eat-this",
  "version": "2.0.0",
  "private": true,
  "description": "EAT THIS — Berlin Food Guide (Next.js app and Sanity Studio)",
  "author": "EAT THIS",
  "type": "module",
  "engines": {
    "node": ">=20.19 <21"
  },
  "scripts": {
    "dev": "npm run dev --prefix nextjs",
    "build": "npm run build --prefix nextjs",
    "test": "npm test --prefix nextjs",
    "lint": "npm run lint --prefix nextjs"
  },
  "devDependencies": {
    "prettier": "^3.8.2"
  }
}
